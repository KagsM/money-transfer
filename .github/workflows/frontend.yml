name: Frontend CI/CD

on:
  push:
    branches: [ main, development ]
  pull_request:
    branches: [ main, development ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test --if-present

      - name: Build
        run: npm run build

      - name: Deploy with Vercel CLI
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_SECRET_TOKEN }}
        run: |
          npm install -g vercel
          vercel --token "${{ secrets.VERCEL_SECRET_TOKEN }}" --prod --confirm --yes

      - name: Update Project Board Status to "Deployed"
        if: success()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          echo "Updating GitHub Project board status..."
          gh project item-edit --id $(gh project item-list --project "4" --limit 1 --json id --jq '.[0].id') \
            --field "Status" --value "Deployed"

      - name: Notify Slack
        if: always()
        uses: slackapi/slack-github-action@v1.26.0
        with:
          payload: |
            {
              "text": ":rocket: *Frontend CI/CD* - ${{ job.status }} for *${{ github.repository }}* on *${{ github.ref_name }}* by *${{ github.actor }}*"
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }} 

      - name: Comment on PR with Deploy Status
        if: always()
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          message: |
            🚀 **Deployment Status:** ${{ job.status }}
            🔗 Vercel URL: [View Deployment](https://vercel.com/dashboard)



